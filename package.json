{
  "name": "gistify",
  "version": "1.0.0",
  "main": "index.js",
  "author": "Harry Nicholls <harry.m.nicholls@gmail.com>",
  "license": "MIT",
  "scripts": {
    "copy": "cp -R images manifest.json src/popup.html build/",
    "copy:build-local": "yarn run copy && cp .env.local build/.env",
    "dev": "parcel build src/popup.js src/background.js -d build --no-content-hash --no-minify && yarn run copy",
    "build": "parcel build src/popup.js src/background.js -d build --no-content-hash && yarn run copy",
    "build:local": "parcel build src/popup.js src/background.js -d build --no-content-hash --no-minify && yarn run copy:build-local",
    "package": "rm -f get-the-gist.zip && zip -r get-the-gist.zip build"
  },
  "dependencies": {
    "@aduh95/preact-fontawesome": "^0.1.4",
    "@babel/plugin-syntax-async-generators": "^7.2.0",
    "@babel/plugin-transform-react-jsx": "^7.3.0",
    "@fortawesome/fontawesome-svg-core": "^1.2.22",
    "@fortawesome/free-regular-svg-icons": "^5.10.2",
    "@fortawesome/free-solid-svg-icons": "^5.10.2",
    "@octokit/rest": "^16.28.7",
    "copy-to-clipboard": "^3.2.0",
    "preact": "^10.0.0-rc.1",
    "query-string": "^6.8.2",
    "uuid": "^3.3.3"
  },
  "devDependencies": {
    "@babel/core": "^7.5.5",
    "parcel-bundler": "^1.12.3"
  }
}
